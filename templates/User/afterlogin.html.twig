{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="mt-5">Zjedzone kcal / zapotrzebowanie kcal</h1>
            <br>
            <br>
            <br>
            <div class="col-lg-6">
                <form action="{{ path('uploadFood') }}" method="POST" class="form-horizontal">
                    <label for="username">Szukaj produktu</label>
                    <input type="text" name="search" class="form-control"><br>
                    <button type="submit" class="btn btn-success">Dodaj</button><br>                   
                </form>
            </div>
            <div class="col-lg-6">
                <form action="{{ path('uploadFood') }}" method="POST" class="form-horizontal">
                    <h3 class="mt-5">Wybrany posiłek</h3>
                    {{ findFood() }}

                </form>
                
            </div>
        </div>
    </div>
{% endblock %}

