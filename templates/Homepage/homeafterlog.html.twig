{% extends 'base.html.twig' %}

{% block body %}
    <div class="row" id="dashboard">
        <div class="col-lg-12">
            {#{% if (preferentions.kcal_day) is empty  %}
                <p> Ustaw preferencje, aby wieświetlic kalorie</p>
            {% endif %} #}
            {% if preferentions %}
                {% for preferention in preferentions %}   
                    {% set result = preferention.kcal_day - summKcal %}           
                    <h1 class="mt-5"><span style="color: blue">{{ preferention.kcal_day|e }}</span>
                    -
                     <span style="color: red">{{ summKcal|number_format }}</span>
                    =
                      <span style="color: green">{{ result }}</span> </h1><br><br><br>

                    Białko: {{ summProtein|number_format }}/{{ preferention.proteinPerDay|e }} <br>
                    Tłuszcz: {{ summFat|number_format }}/{{ preferention.fatPerDay|e }} <br>
                    Węglowodany: {{ summCarbo|number_format }}/{{ preferention.carboPerDay|e }}

                    {% endfor %}

            {% else %}
                <form action="{{ path('preferentions') }}" method="POST" class="form-horizontal">
                    <h1 class="mt-5">Oblicz zapotrzebownie kaloryczne :)</h1><br>
                    <button type="submit" class="btn btn-success">Dalej</button>
                 </form>

            {% endif %}
            
            <div class="col-lg-6">
                <form action="{{ path('findFood') }}" method="POST" class="form-horizontal">
                    
            </div>
        </div>
    </div>
{% endblock %}



