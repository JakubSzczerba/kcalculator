{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="mt-5">Strona główna</h1><br>
            <div class="col-lg-6">
                <form action="{{ path('uploadFood') }}" method="POST" class="form-horizontal">
                    <h3 class="mt-5">Zjedzone kcal / zapotrzebowane </h3><br>
            </div>
        </div>
    </div>
{% endblock %}
